[project]
name = "arcnet-workspace"
version = "0.0.0"
description = "ArcNet monorepo workspace root"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["sdk", "server", "agents"]

[tool.uv]
package = false
