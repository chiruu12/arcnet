export function App() {
  return (
    <main style={{ fontFamily: "ui-monospace, monospace", padding: 24 }}>
      <h1>{"> arcnet"}</h1>
      <p>hq shell — Fleet Health + Time Machine land in Phase 3/5.</p>
    </main>
  );
}
