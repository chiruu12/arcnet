"""Demo agents + Bug Suite — demo layer only (never imported by sdk/server/hq)."""

from arcnet_agents.agent_j import build_agent_j, build_fleet_clone

__all__ = ["build_agent_j", "build_fleet_clone"]
