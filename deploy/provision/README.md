# provision/

- `agno-dashboard.json` — official SigNoz Agno dashboard template (from SigNoz/dashboards). Import in UI: Dashboards → New → Import JSON. Automated import lands in Phase 2 `setup.py`.
- Custom Fleet Ops / Threats & Trust / Cost & Tokens dashboards — authored in Phase 2 against real OpenInference keys recorded in `docs/04-signoz-integration.md`.
