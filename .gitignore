.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
node_modules/
hq/dist/
hq/.vite/
*.egg-info/
.uv/
# deploy/casting.yaml.lock is intentionally TRACKED: judges re-run
# `foundryctl forge -f deploy/casting.yaml` against it to reproduce the SigNoz
# deployment. deploy/pours/ stays ignored — it is regenerated output and the
# compose file there has local credentials injected into it.
deploy/pours/
deploy/provision/.dashboard-ids.json
*.db
data/
.coverage
htmlcov/
docs/_phase0_*.jsonl
docs/_phase0_*.json
.pnpm-store/
deploy/mcp/bin/
hq/tsconfig.tsbuildinfo
.signoz-local-admin

# local scratch (logs, probes, ad-hoc json)
tmp/
